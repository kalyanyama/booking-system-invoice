const contactPageMutations = require("./mutations");
const contactPageQueries = require("./queries");
const contactPageTypeDefs = require("./typedefs");

module.exports = {
  contactPageMutations,
  contactPageQueries,
  contactPageTypeDefs,
};
